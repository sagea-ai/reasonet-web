/**
 * Defines the available sort options for data sorting operations
 */
export type SortOption = 'asc' | 'desc' | 'none' | 'name' | 'updated' | 'stars' | 'forks' | 'language';

/**
 * Defines the available filter options for data filtering operations
 */
export type FilterOption = 'all' | 'public' | 'private' | 'none';

/**
 * Defines the available time range options for filtering data
 */
export type TimeRangeOption = 'this-week' | 'last-7-days' | 'last-30-days' | 'last-3-months' | 'last-year' | 'all-time';

